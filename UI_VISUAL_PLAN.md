# План визуальной переработки интерфейсов

Документ является постоянной очередью интерфейсной миграции. Каждый этап завершается визуальным эталоном, автотестами, patch-версией и публикацией в GitHub.

## Художественный контракт

- Эталон — стартовый экран и рюкзак: резное дерево, пергамент, латунь, ткань и неброские растительные детали.
- Все runtime-элементы собираются из независимых PNG или nine-patch; соседние ячейки атласа не должны попадать в спрайт.
- Базовый viewport `1152×648`, сетка отступов `8 px`, минимальная интерактивная область `48×48 px`.
- Фокус подсвечивает изображение изнутри, а не рисует внешнюю жёлтую рамку.
- Длинные переводы, мышь, клавиатура, геймпад, тачскрин и reduced motion входят в обязательный контракт.

## Очередь релизов

- [x] `UI-001` — UI Bible и первый модульный UI Kit из 16 независимых компонентов.
- [x] `UI-002` — компактный HUD, единые часы/репутация, контекстные эффекты, чистые safe-area и быстрая панель рюкзака.
- [x] `UI-003` — пауза, настройки, сохранения, выбор языка, подтверждения и поражение.
- [x] `UI-004` — общий каркас инвентаря, сундука, магазина, крафта, кузницы и экипировки.
- [x] `UI-005` — диалоги, журнал сюжетных/побочных заданий, обучение и уведомления о наградах.
- [x] `UI-006` — персонаж, характеристики, эффекты, компаньоны и художественное дерево способностей.
- [x] `UI-007` — карта, календарь, погода, бестиарий, коллекции и справочник рецептов.
- [x] `UI-008` — переходы, focus/pressed/disabled-анимации, частицы и семейство UI-звуков.
- [x] `UI-009` — адаптивность Full HD/2K/4K, safe-area, масштаб текста, геймпад и тачскрин.
- [x] `UI-010` — художественно связанный debug UI и редактор уровней с технической цветовой кодировкой.
- [x] `UI-011` — эталонные скриншоты, переполнение локализаций и полный визуальный regression gate.
- [x] `UI-012` — закрытие оставшихся runtime-островков: рыбалка, создание героя, фермерский справочник, мини-карта и цели главы используют общий storybook-kit; 26 эталонов нормализованы до `1152×648`.

## Visual regression gate

- `assets/generated/ui/visual_regression_manifest.json` фиксирует размер `1152×648` и SHA-256 каждого из 26 принятых UI-эталонов.
- Автотест отклоняет отсутствующий, пустой, повреждённый, непропорциональный или неявно заменённый кадр.
- Отдельная метрика различает реальные изменения двух кадров одинакового разрешения.
- Ключевые кнопки проверяются во всех локалях при максимальном масштабе текста реальным игровым шрифтом.
- Намеренное визуальное изменение требует переснять экран, проверить его глазами и явно обновить запись манифеста.
- После принятой пересъёмки `python3 tools/update_ui_visual_manifest.py` обновляет размеры и хеши только уже зарегистрированных кадров, не добавляя экран молча.

## Критерий завершения экрана

1. Геометрия и hit-зоны берутся из одного источника.
2. Все иконки центрируются в safe-area без искажения пропорций.
3. Ни один декоративный элемент не масштабируется целиком там, где требуется nine-patch.
4. Экран остаётся читаемым во всех локалях и режимах управления.
5. Есть screenshot-эталон и тесты поведения/геометрии.
